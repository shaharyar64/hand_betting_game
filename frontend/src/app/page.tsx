/** Application module. */
import { PremiumLandingPage } from "@/components";

/** Render the landing page as the root route content. */
export default function Home() {
  return <PremiumLandingPage />;
}
